// Better Auth Client Configuration
// This file initializes the client-side authentication library client for React/Next.js components.
import { createAuthClient } from "better-auth/react"

// Creates and exports the auth client to perform authentication operations (signIn, signUp, signOut, etc.) from client components.
export const authClient = createAuthClient({})